# =============================================================
# Variáveis de ambiente — copie para .env.local e preencha
# =============================================================

# DeepSeek (IA — compatível com OpenAI)
# Aceita também os nomes DEEPSEEK_API_KEY ou VITE_API_KEY
API_KEY=

# Supabase
# URL do projeto (sem /rest/v1 — o cliente acrescenta sozinho)
VITE_SUPABASE_URL=
# Chave publishable / anon
VITE_SUPABASE_ANON_KEY=
